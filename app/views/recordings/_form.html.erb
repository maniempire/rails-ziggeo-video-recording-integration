<%= form_for(@recording) do |f| %>
  <% if @recording.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@recording.errors.count, "error") %> prohibited this recording from being saved:</h2>

      <ul>
      <% @recording.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :sender_email %><br>
    <%= f.text_field :sender_email %>
  </div>
  <div class="field">
    <%= f.label :sender_video_url %><br>
    <%= f.text_field :sender_video_url %>
  </div>
  <div class="field">
    <%= f.label :receiver_email %><br>
    <%= f.text_field :receiver_email %>
  </div>
  <div class="field">
    <%= f.label :receiver_video_url %><br>
    <%= f.text_field :receiver_video_url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
